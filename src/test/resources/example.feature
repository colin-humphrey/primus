@accepted
Feature: This is an example feature file.  It works with the ExamplePackage.
  In particular the files.  ExampleSteps which uses ExampleActions.

  @accepted
  Scenario: Do we have a scenario?
    Given I want to check if this scenario exists